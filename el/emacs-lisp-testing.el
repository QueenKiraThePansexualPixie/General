;; emacs-lisp-testing.el

; --- outline ---
; # functions...6
; # macros.....19*
; # key........23*

; --- functions ---

(defun dum-dum
      "A stupid function that has no real purpose.
      
      This function attempts to simplify verbose text.
      
      For each word in the verbose text; find a suitable simplification, else leave it."
      (smarty)
      (+ 1 1)) ;;; <- Placeholder functionality.

; --- macros ---

; (define macro)

; --- key ---
; * (in outline) = subject to change
